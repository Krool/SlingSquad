{
  "name": "slingsquad",
  "version": "1.0.0",
  "private": true,
  "homepage": "https://krool.github.io/SlingSquad/",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build && npm run copy-assets",
    "copy-assets": "cp -r assets/sprites dist/assets/",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "phaser": "^3.88.2"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "gh-pages": "^6.3.0",
    "typescript": "^5.7.2",
    "vite": "^6.3.0"
  }
}
